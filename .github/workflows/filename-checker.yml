name: Check File Name

on:
  pull_request:
    types:
      - synchronize
      - opened
      - reopened
  push:
    branches:
      - main

jobs:
  check_file_name:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Get changed files
      id: file_changes
      run: |
        git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.event.pull_request.head.sha }} > changed_files.txt
        
    - name: Read changed files
      id: read_changed_files
      run: |
        echo "::set-output name=files::$(cat changed_files.txt)"
        
    - name: Check file names
      run: |
        for file in ${{ steps.read_changed_files.outputs.files }}
        do
          if echo "$file" | grep -q '[^a-z0-9_\-./]'; then
            echo "File name contains invalid characters: $file"
            echo "::error file=$file::File name contains invalid characters"
            exit 1
          fi
        done
